#include <iostream>
using namespace std;
int main()
{
    float a, b, h;
    cout << "Enter Base and Height of a trinagle ";
    cin >> b >> h;
    a = (b * h) / 2;
    cout << "Area of trinagle is " << a << endl;
    return 0;
}